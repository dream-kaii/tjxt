<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tianji.remark.mapper.LikedRecordMapper">

    <!--
       批量插入或更新点赞记录（UPSERT）
       说明：
       - (biz_id, user_id) 联合唯一索引，当插入冲突时更新 biz_type 和 update_time
       - 用于 Redis 脏数据定时同步到 MySQL 的场景
   -->
    <insert id="batchUpsert">
        INSERT INTO liked_record (user_id, biz_id, biz_type, create_time, update_time)
        VALUES
        <foreach collection="records" item="r" separator=",">
            (#{r.userId}, #{r.bizId}, #{r.bizType}, #{r.createTime}, NOW())
        </foreach>
        ON DUPLICATE KEY UPDATE
            biz_type = VALUES(biz_type),
            update_time = NOW()
    </insert>

</mapper>
